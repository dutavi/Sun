<h3> User</h3>
<p>
  Name: <%= @user.name if @user.name %>
<p>
  Email: <%= @user.email if @user.email %> </p>
<section class="stats">
        <%= render 'shared/stats' %>
</section>
<%= render 'follow' %>

